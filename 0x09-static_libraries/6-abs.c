#include "main.h"

/**
 * _abs - used to calculate the absolute value of an integer
 * @n: integer to be checked
 * Return: absolute value of integer found
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
